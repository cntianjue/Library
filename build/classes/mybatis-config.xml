<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration >
  <!-- 指定采用的日志类型：log4j sef4j -->
    <properties  resource="log4j.properties"></properties> 
  <settings>
  	<setting name="logImpl" value="LOG4J"/>
  	<!-- 开启二级缓存 -->
  	<setting name="cacheEnabled" value="true"/>
  </settings>
 <!--  <environments default="development">
    <environment id="development">
      指定怎样管理事务：使用JDBC原生的事务管理器管理事务
      <transactionManager type="JDBC"/>
      <dataSource type="POOLED">
        <property name="driver" value="${jdbc.driver}"/>
        <property name="url" value="${jdbc.url}"/>
        <property name="username" value="${jdbc.username}"/>
        <property name="password" value="${jdbc.password}"/>
      </dataSource>
    </environment>
  </environments>
  <mappers>
      <mapper class="com.library.sql.sqlPro"/> 
    <mapper class="com.library.book.dao.DAO"/> 
    <mapper resource="com/library/book/bookMapper/BookMapper.xml"/>

  </mappers> -->
</configuration>